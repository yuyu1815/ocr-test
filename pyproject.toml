[project]
name = "jetson-ocr"
version = "0.1.0"
description = "Receipt OCR on Jetson Nano using LightOnOCR-2-1B"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "torchvision",
    "pillow",
    "pypdfium2",
    "accelerate",
    "protobuf",
    "scipy",
    "opencv-python",
    "transformers @ git+https://github.com/huggingface/transformers",
]
